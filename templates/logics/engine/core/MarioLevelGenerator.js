
class MarioLevelGenerator {
    /**
     * Generate a playable mario level
     *
     * @param model contain a model of the level
     */
    // getGeneratedLevel(model, timer);

    /**
     * Return the name of the level generator
     *
     * @return the name of the level generator
     */
    // getGeneratorName();
}
